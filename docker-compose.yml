
services:
  ai-service:
    build: . 
    container_name: document-classification-flask
    ports:
      - "${OCR_SERVICE_PORT:-5006}:5006"
    environment:
      TZ: Asia/Shanghai
    volumes:
      - /opt/paddleocr_cache:/home/appuser/.paddleocr
    restart: unless-stopped
